## Portfolio Analysis

To run the project:

1. Sync dependencies:

```bash
uv sync 
```

2. Activate venv:

```bash
source .venv/bin/activate
```

3. Install dependencies:

```bash
uv pip install -e . 
```